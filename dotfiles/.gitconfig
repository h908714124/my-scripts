[alias]
	st = status
	co = checkout
	ci = commit
	br = branch --sort=committerdate
	merge = merge --ff-only
[user]
	name = jbock
	email = jbock-java@gmx.de
[merge]
	tool = meld
[credential]
	helper = cache --timeout=3600
[core]
	pager = less -F -X
	longpaths = true
[pull]
	ff = only
#[http]
#  proxy = my-proxy.com:8080
#  sslVerify = false

