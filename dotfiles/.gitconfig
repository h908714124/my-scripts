[alias]
	st = status
	co = checkout
	ci = commit
	br = branch --sort=committerdate
[user]
	name = jbock
	email = jbock-java@gmx.de
[merge]
	tool = meld
[rerere]
        enabled = true
[credential]
        helper = cache --timeout=3600
[core]
        pager = less -F -X
	longpaths = true
[pull]
        ff = only
